import React from 'react';
import QuizList from './QuizList';

const Quiz = () => {
    return(
        <section className = 'quiz'>
            <QuizList />
        </section>
    )
}

export default Quiz;
